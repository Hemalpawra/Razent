/**
 * Customer screens placeholder boundary.
 * Per AI_RULES.md: 9 future screens listed for Supabase-ready growth.
 * Not needed for the first 4 (Dashboard, Products, Orders, Analytics)
 * but kept as an architecture boundary.
 */
export const customerScreenPlaceholder = () => null

/**
 * Future customer screens (AI_RULES §1 + §5):
 * StoreHome / ProductListing / ProductDetail / AICartDrawer /
 * Checkout / CheckoutSuccess / OrderTracking / AIChat / CustomerProfile
 */